<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="total_amount_less_wht" inherit_id="account.report_invoice_document">
        <xpath expr="//div[hasclass('page')]/h2" position="replace">
             <h2>
                <span t-if="o.move_type == 'out_invoice' and o.state == 'posted'"> Tax Invoice</span>
                <span t-if="o.move_type == 'out_invoice' and o.state == 'draft'">Draft Invoice</span>
                <span t-if="o.move_type == 'out_invoice' and o.state == 'cancel'">Cancelled Invoice</span>
                <span t-if="o.move_type == 'out_refund'">Credit Note</span>
                <span t-if="o.move_type == 'in_refund'">Vendor Credit Note</span>
                <span t-if="o.move_type == 'in_invoice'">Vendor Bill</span>
                <span t-if="o.name != '/'" t-field="o.name"/>
             </h2>
        </xpath>

        <xpath expr="//div[@id='total']" position="after">
            <t t-if="'Withholding Tax' in [group['tax_group_name'] for group in o.tax_totals['groups_by_subtotal']['Untaxed Amount']]">
                <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ms-auto">
                    <table class="table table-sm table-borderless" style="page-break-inside: avoid;">
                        <tr class="border-black o_total">
                            <td>Amount To Pay(Less Withholding)</td>
                            <td class="text-end">
                                <t t-foreach="o.tax_totals['groups_by_subtotal']['Untaxed Amount']" t-as="group">
                                    <t t-if="group['tax_group_name'] != 'Withholding Tax'">
                                        <t t-set="total"
                                           t-value="o.tax_totals['amount_untaxed'] + group['tax_group_amount']"/>
                                        <span t-esc="total"
                                              t-options='{"widget": "monetary", "display_currency": o.currency_id}'/>
                                    </t>
                                </t>
                            </td>
                        </tr>
                    </table>
                </div>
            </t>
        </xpath>
    </template>
</odoo>
